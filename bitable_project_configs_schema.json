{
  "version": "v1",
  "table_name": "ProjectConfigs",
  "description": "Per-project metadata persisted as the single source of truth for Coordinator project_configs.",
  "fields": [
    {
      "name": "项目名",
      "field_type": "text",
      "required": true,
      "description": "项目唯一主键(对应 ProjectConfig.project)。"
    },
    {
      "name": "类别",
      "field_type": "text",
      "required": true,
      "description": "项目类别,如 backend / frontend / ai-agent。"
    },
    {
      "name": "模板版本",
      "field_type": "text",
      "required": true,
      "description": "ARCHITECTURE 模板版本标识。"
    },
    {
      "name": "架构文档ID",
      "field_type": "text",
      "required": true,
      "description": "飞书 ARCHITECTURE 文档的 document_id。"
    },
    {
      "name": "架构文档URL",
      "field_type": "text",
      "required": true,
      "description": "飞书 ARCHITECTURE 文档的完整 URL。"
    },
    {
      "name": "技术栈JSON",
      "field_type": "text",
      "required": false,
      "description": "tech_stack 字段的 JSON 字符串(dict[str, str])。"
    },
    {
      "name": "设计系统文档ID",
      "field_type": "text",
      "required": false,
      "description": "该项目的设计系统文档 document_id(可空)。"
    },
    {
      "name": "GitHub仓库URL",
      "field_type": "text",
      "required": false,
      "description": "项目 GitHub 仓库 URL;为空表示新项目,Coordinator 在首次 Spec 转化时基于 harness-scaffold 初始化。"
    },
    {
      "name": "GitHub负责人用户名",
      "field_type": "text",
      "required": false,
      "description": "项目 GitHub admin 用户名,用于新建 repo 时授予 admin 权限。"
    },
    {
      "name": "引导状态",
      "field_type": "text",
      "required": false,
      "description": "Bootstrap 状态机: UNBOOTSTRAPPED / BOOTSTRAPPING / PROVISIONED / FAILED / ARCHIVED / DECOMMISSIONED。"
    },
    {
      "name": "引导日志JSON",
      "field_type": "text",
      "required": false,
      "description": "bootstrap_log 的 JSON 字符串(list[dict]);每步执行情况记录。"
    },
    {
      "name": "引导完成时间",
      "field_type": "text",
      "required": false,
      "description": "Bootstrap 成功进入 PROVISIONED 的 ISO 时间戳;未完成时为空。"
    },
    {
      "name": "项目状态",
      "field_type": "text",
      "required": false,
      "description": "项目生命周期状态(MVP 等同引导状态;Phase 2 引入 ARCHIVED/DECOMMISSIONED)。"
    },
    {
      "name": "飞书群chat_id",
      "field_type": "text",
      "required": false,
      "description": "Bootstrap 创建的项目群 chat_id(用于后续 needs_ui/通告等)。"
    }
  ]
}
